// third-party adapter (e.g. supabase, rest-api, locale, shadcn)
//
// pkg/<adapter>/ wraps an external SDK with project-specific config.
// Export only what consumers need; never re-export raw SDK internals.
